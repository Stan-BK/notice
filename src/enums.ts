export const enum VapidKeys {
	PrivateKey = 'VAPID_PRIVATE_KEY',
	PublicKey = 'VAPID_PUBLIC_KEY',
}

export const enum NoticeType {
	Tomorrow = 'tomorrow',
	Today = 'today',
	Yesterday = 'yesterday',
	All = 'all',
}

export const enum NoticeOperation {
	Add = 'add',
	Remove = 'remove',
}
